import React from "react";
import InputNumber from "rc-input-number";
import {Slider} from "@material-ui/core";

export const NumberInput = ({value, onInputChange,checkAnswer}) => {


    return(
        <div>

        </div>
    )
}